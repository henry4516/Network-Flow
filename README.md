The Graphs.py well defined graph data structures as well as relevant functions. The implementation preference is edge list.

Main functions are:
find_dipath: find a dipath between nodes i and j if any. Returns False if none exists.
all_dipaths: find all possible dipaths between nodes i and j. Returns an empty list if none exists.
